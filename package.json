{
  "name": "sanitize-comments-fail-closed",
  "version": "1.0.0",
  "description": "Mask comments in JS by default (fail-closed), verify with an independent parser, gate with a real import test",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "acorn": "8.15.0",
    "meriyah": "6.0.6"
  }
}